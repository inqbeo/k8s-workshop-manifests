# Block 5 — Secret (sensitive config). stringData is plain text on apply;
# stored base64-encoded (NOT encrypted) -> see the base64 excursion in the deck.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  labels:
    app: todo
type: Opaque
stringData:
  AUTH_PASSWORD: "lab-secret"
  # DATABASE_URL is added in Block 6 (09-switch-to-postgres.yaml)
